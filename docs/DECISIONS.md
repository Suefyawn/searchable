# Architecture Decision Records

Format: **ADR-n · Title**, Status · Date. Context → Decision → Consequences.

---

## ADR-1 · One Next.js app, no separate backend
**Accepted · 2026-09-15**
Context: Solo founder; site + admin + API + jobs. Decision: single Next.js 16 App Router codebase; Server Components for reads, Server Actions for writes, route handlers for external calls. Consequences: one deploy, one type system, no API contract drift. Revisit only if a non-JS consumer needs a stable API (then expose `/api/v1`).

## ADR-2 · Postgres is the only database; Drizzle is the ORM
**Accepted · 2026-09-15**
Context: Need FTS, JSONB, generated columns, real migrations, and a path to Supabase. Decision: Drizzle with the `postgresql` dialect; schema in TypeScript; SQL migrations committed under `drizzle/`. Consequences: portable across any Postgres host; no vendor SDK in domain code.

## ADR-3 · PGlite for local development instead of Docker/Supabase-local
**Accepted · 2026-09-15**
Context: Docker is not installed on the dev machine; the founder wants to build fully locally before opening any accounts. Decision: `@electric-sql/pglite` (Postgres compiled to WASM) persisted at `.data/pglite`, selected via `DATABASE_URL=pglite://…`. Consequences: zero-install, real Postgres semantics, same Drizzle code as production. Limits: single connection, no PostGIS (use lat/lng + haversine until Supabase), no Supabase Auth locally (→ ADR-4). If Docker is installed later, `supabase start` is a drop-in alternative; nothing in the app changes.

## ADR-4 · better-auth instead of Supabase Auth
**Accepted · 2026-09-15**
Context: ADR-3 removes Supabase Auth locally; two auth systems would be worse than one. Decision: better-auth with the Drizzle adapter; email/password first, OAuth (Google) later; `users.role` for authorisation. Supabase is used as managed Postgres (+ Storage), not as an auth provider. Consequences: identical auth locally and in prod; portable. Trade-off: no Supabase RLS-based row security, authorisation lives in server code (`requireRole`), which is where it belongs for an RSC app that never exposes the DB to the browser.

## ADR-5 · Search is a single write-through index table
**Accepted · 2026-09-15**
Context: Federated search across 6+ entity types must exist on Day 1. Decision: `search_documents(entity_type, entity_id, url, title, summary, body, city, category, tsv, boost, published_at)` with a generated `tsvector`; every mutation upserts its document; ranking in SQL. Consequences: one query, one ranking function, trivially re-indexable; swappable for Typesense/Meili by re-implementing `src/lib/search.ts` only.

## ADR-6 · Tools are code, configured by versioned data
**Accepted · 2026-09-15**
Context: 300 calculators cannot be hand-built pages; tax tables change yearly. Decision: `ToolDefinition` interface; generic renderer; rate tables in `src/tools/data/*` with `effectiveFrom` + `source`; tool metadata mirrored into `tools` table for search/SEO. Consequences: a new tool is one file; a FY change is a data edit + review date bump.

## ADR-7 · Articles: one table, `kind` column
**Accepted · 2026-09-15**
Context: news, guides, explainers share 90% of fields and the same workflow. Decision: single `articles` table with `kind` enum and `categories.kind` scoping. Consequences: one editor, one search doc type with sub-type boost, simple cross-linking.

## ADR-8 · Locations are a self-referential tree
**Accepted · 2026-09-15**
Decision: `locations(id, parent_id, kind: country|province|city|area, slug, name, lat, lng)`. City × category pages resolve `city` from this table. Consequences: areas, districts, tehsils fit without schema changes.

## ADR-9 · Entities as first-class knowledge graph nodes
**Accepted · 2026-09-15**
Decision: `entities` + polymorphic `entity_links(entity_id, target_type, target_id, relation)`. Consequences: entity hubs and later AI grounding come for free from linking discipline in the editor.

## ADR-10 · No dates in URLs; `/b/[slug]` for businesses
**Accepted · 2026-09-15**, see `docs/URL-ARCHITECTURE.md`.

## ADR-11 · npm, not pnpm/yarn
**Accepted · 2026-09-15**, already installed; nothing else to set up. Revisit if the repo becomes a monorepo.

## ADR-12 · Design system is hand-rolled primitives, shadcn-compatible
**Accepted · 2026-09-15**
Context: The user asked for a clean, modern, highly readable UI. Decision: Tailwind v4 tokens in `globals.css`; small set of primitives in `src/components/ui` following shadcn conventions (`cn()`, variants) so shadcn components can be dropped in later without restyling. Consequences: no CLI dependency in the scaffold; full control of typography and spacing.

## ADR-13 · `marked` for Markdown, `@tailwindcss/typography` for prose
**Accepted · 2026-09-15**
Context: Articles and guides are authored in Markdown by trusted editors; the site needs excellent reading typography. Decision: `marked` (small, fast, GFM) renders on the server; `@tailwindcss/typography` `prose` classes style the output with our tokens. Consequences: no client-side Markdown; editor content is trusted (admin-only), if user-generated Markdown is ever rendered, add sanitisation (`DOMPurify`/`rehype-sanitize`) first.

## ADR-14 · Visual redesign: display typeface, soft surfaces, glass header
**Accepted · 2026-09-15**
Context: The founder judged the first pass "old fashioned". Decision: Bricolage Grotesque (display, variable opsz/wdth) for headings + Inter for body; warm off-white canvas with radial gradient glows on the hero; borderless cards with layered shadows and hover lift; pill buttons and chips; floating glass header; dark editorial footer; per-category colour accents for tools; live data ticker on the home page. Tokens live in `globals.css` (`surface`, `surface-hover`, `glass`, `eyebrow`, `text-gradient`, `hero-bg`). Consequences: every page inherits the language through primitives; dark mode is preserved via the same tokens; motion respects `prefers-reduced-motion`.

## ADR-15 · Minimal news design (supersedes ADR-14)
**Accepted · 2026-09-15**
Context: The founder wants Searchable to look like a news website, minimalistic and easy on the eyes, and rejected rounded buttons/fields and glossy effects as old-fashioned. Decision: Newsreader (serif) for headlines and deks; Inter for article body, UI and meta (founder preference: body must be sans). Square corners everywhere (all radius tokens are 0). Hairlines instead of cards and shadows; a heavier rule above section titles; small-caps sans labels; one quiet green accent for labels and links; near-black primary buttons; a classic masthead (date line, wordmark, section nav, small search) and a light footer. The home page is a front page: search bar, numbers strip, lead story + secondary column + latest headlines, calculators, guides, directory. Consequences: no gradients, glass, pills or entrance animations anywhere; new components should use `hairline`, `rule`, `eyebrow`, `headline-link`, `surface` (white + 1px border) from `globals.css`.

## ADR-16 · Image uploads: sharp → WebP, local disk now, Supabase Storage later
**Accepted · 2026-09-15**
Decision: `POST /api/upload` accepts JPEG/PNG/WebP/GIF/AVIF ≤ 8 MB, normalises with `sharp` (rotate, resize per variant: article 1800px, cover 2000px, photo 1600px, logo 512px; WebP q82; metadata stripped), records a `media` row, and stores via `putObject()`, `STORAGE_PROVIDER=local` writes `public/uploads/YYYY/MM/<uuid>.webp`; `supabase` uploads to the `media` bucket over the Storage REST API. Editors may upload anything; business owners only for businesses they manage. Consequences: one adapter to switch at go-live; images served through `next/image` with `remotePatterns` already open for the Supabase host.

## ADR-17 · Openly licensed photography via Openverse; never AI-generated imagery
**Accepted · 2026-09-15**
Context: The founder wants real, relevant photos and no AI-generated images anywhere. Decision: photos come from the Openverse API (Flickr, Wikimedia Commons, museums) restricted to CC0 / public domain / CC BY / CC BY-SA; every import stores creator, licence and source page in `media` and renders a credit line under the image. Editors pick photos in the article editor; `scripts/seed-images.ts` seeds articles, cities and categories. Consequences: attribution is a first-class field (`featuredImageCredit`, `imageCredit`); the Wikimedia API is unreachable from some networks but the image hosts are, so Openverse is the single search path; anonymous limit 200/day, register a client id at go-live.

## ADR-18 · Monetization: manual invoices first, gateway later; placement is sold, editorial never is
**Accepted · 2026-09-15**
Decision: `orders` (business plans, sponsored posts, placements) and `submissions` (guest / sponsored / press release) tables; prices in `src/content/pricing.ts`; invoices `SP-YYYY-NNNNNN` with bank / JazzCash / Easypaisa details; admin marks paid, which sets tier, expiry and verification; cron lapses plans. Sponsored articles carry a disclosure block, a Sponsored badge and `rel="sponsored"` on outbound links; free listings link `nofollow`, paid tiers follow. AdSense loads only when `NEXT_PUBLIC_ADSENSE_CLIENT` is set and never inside tools or search. Consequences: a card gateway (Safepay/PayFast) is another `provider` value on the same order; see `docs/MONETIZATION.md`.

## ADR-19 · Directory growth: CSV import, dedupe by phone/name/geo, area pages, click-to-load OSM map
**Accepted · 2026-09-15**
Decision: `src/lib/import.ts` parses CSV, validates rows, normalises phones to +92, resolves category/city/area by slug or name (with aliases), runs `findDuplicates()` (same phone → same name key in city → Dice ≥ 0.8 within 300 m / ≥ 0.9 in city) and previews before committing to the review queue. Admin can merge a duplicate (status `duplicate`, reviews/leads moved, 301 redirect). Area pages `/businesses/[category]/[city]/[area]` and `/cities/[city]/[area]` are `noindex` under 5 listings. Maps are a click-to-load OpenStreetMap iframe on profiles, no map library until local search needs multi-pin maps (MapLibre then). Consequences: `pg_trgm` is not required; dedupe runs in JS over the city's rows, which is fine to ~20k listings per city.

## ADR-20 · Light theme only; hero-first home page without a search box
**Accepted · 2026-09-15**
Context: The founder wants black-on-white, minimal and modern, and judged the search-box-first home "not suitable" and lacking a hero. Decision: the site ships light only, `--bg: #ffffff`, near-black type; the dark palette stays in `globals.css` but applies only under an explicit `data-theme="dark"` (Tailwind `dark:` is remapped to that attribute, the OS preference is ignored). Home: a thin scrolling numbers ticker, then a hero of the lead story (photo 7/12, headline stack 5/12 with three text headlines), then a four-up photo grid, calculators with example results, guides with thumbnails, directory, cities, newsletter. Search lives in the masthead only. Consequences: no per-component `dark:` classes are needed for new work; a theme toggle can be added later by setting the attribute.

## ADR-21 · Automated data ingestion from public sources with jump guards
**Accepted · 2026-09-15**
Decision: `src/lib/ingest.ts` pulls the data hub's core series daily (`/api/cron/ingest`, also "Fetch now" in `/admin/data`): SBP's economic-data page (USD/PKR M2M, 12-month KIBOR offer, policy rate), open.er-api.com cross rates for AED/SAR/GBP/EUR anchored on SBP's USD/PKR, PSO's fuel-price page (Premier Euro 5, Hi-Cetane diesel), and spot gold/silver from gold-api.com converted to per-tola PKR. A reading that moves more than 30% from the last stored value is held ("rejected") rather than published, so a parser break cannot poison the site; an editor can force it. Every auto reading carries a note naming the source and "(auto)". OGRA notifications remain the legal source for fuel prices and are PDFs; PSO's ex-depot price is what consumers pay and is parsed instead. CPI and solar per-watt stay manual. Consequences: seed values are illustrative placeholders replaced on first run; sources are third-party HTML and will need parser upkeep, failures surface in the admin panel, not silently.

## ADR-22 · Front page: story carousel, live feed, press headlines
**Accepted · 2026-09-15**
Context: The founder wants a hero that rotates with breaking stories and "proper news feeds", the site should feel alive between our own publishes, like a top news site. Decision: the hero is a carousel of the five latest photo stories (7-second auto-advance with a thin progress rule, pause on hover, arrows/keyboard, reduced-motion aware; no fades or overlays). Beside it a LIVE panel merges our stories, headlines from seven Pakistani RSS feeds (Dawn, Tribune, Geo, The News, Business Recorder, ProPakistani, ARY) and data-hub readings, refreshed from `/api/feed` every two minutes. A "From Pakistan's press" section on the home and news hub shows headlines by source; only title, source and time are shown and links go to the publisher, no images or body text are reproduced. Feeds are cached 15 minutes via Next's fetch cache and fail soft. Consequences: `src/lib/press.ts` and `src/lib/activity.ts`; the two animations on the front page are the progress rule and the live dot.

## ADR-23 · Monochrome palette
**Accepted · 2026-09-15**
Context: The founder asked why green sat alongside black and white. Decision: the site is black, white and grey. The `brand-*` token ramp is now neutral (so every existing `text-brand-*` / `bg-brand-*` utility renders grey or near-black without touching components); the wordmark's ".pk" is mid-grey; eyebrows, links, the live dot and buttons are ink. The only colour left is functional: red/green arrows on numeric changes in the data ticker and charts. Consequences: no new colour tokens; if an accent is ever wanted, change the `brand-*` ramp in one place.

## ADR-24 · Built for free tiers: CDN-first, R2 images, opportunistic scheduler, email budget
**Accepted · 2026-09-15**
Context: The founder's rule is that going live on Supabase, Vercel, Resend and friends must not run out of free usage; images should sit on Cloudflare. Decision (details in `docs/FREE-TIER.md`): every public page is static or ISR and every API that browsers poll carries `s-maxage`; search suggestions are matched in the browser from one cached JSON file; anonymous visitors never trigger a session request (a readable `sp_auth` hint cookie gates it) and signed-in sessions use better-auth's cookie cache; images are written once with 480/960 px renditions to Cloudflare R2 through a hand-signed S3 PUT (no SDK) and served as plain `<img srcset>` with `images.unoptimized`; crons are daily only, with `runDueJobs()` doing the five-minute work from requests that already happen (guarded by a `settings` row) and an external free pinger recommended; `sendEmail()` enforces daily and monthly counters with a transactional reserve and newsletter sends resume across days; daily pruning keeps analytics and search logs bounded; serverless functions hold one pooled connection. Open question deferred to go-live: Vercel Hobby forbids commercial use, so the host is either Vercel Pro once ads pay for it or Cloudflare Workers via OpenNext (free, commercial allowed); nothing in the code is Vercel-specific except `vercel.json`. Consequences: no `next/image`, no Supabase Storage in production, no 5-minute cron; a new dependency is not needed for R2.

## ADR-25 · Ownership claims with proof tied to the listing, and claim outreach
**Accepted · 2026-09-15**
Context: The directory is pre-listed from public sources; the plan is to email every listed business and ask it to claim, then sell the Verified badge from the dashboard. A claim has to be fool-proof without a paid SMS gateway. Decision (`src/lib/claims.ts`): four proofs, each bound to something already on the listing. (1) Invite link: a signed, 60-day, address-bound token emailed to the listing's address; using it proves control of that mailbox and approves on the spot. (2) Website-domain code: a six-digit code emailed to any mailbox on the listing's website domain (shared providers like gmail are refused), five attempts, 24 hours, auto-approved. (3) Phone: the claimant sends "CLAIM <code>" from the listed number to our WhatsApp/SMS number, or asks for a call; an editor confirms. (4) Document: NTN, registration, utility bill, letterhead or signboard photo, editor-reviewed. Every claim records role, contact and a declaration; one pending claim per user per listing; pending claims expire after 30 days; approval sets the owner, stamps `claimedAt`, rejects competing claims and emails next steps including the Verified badge. Unclaimed profiles say so and invite the claim. Outreach (`/admin/outreach`): a daily drip within the email budget, best-known listings first, one reminder after 14 days, one-click opt-out, plus a per-listing invite link in admin for manual sends. Consequences: migration 0006; `CLAIM_WHATSAPP_NUMBER`; on the free email plan outreach runs at roughly 80 a day.

## ADR-26 · Professional profiles: people, not shops
**Accepted · 2026-09-15**
Context: The founder wants doctors, electricians, engineers, architects and other professionals to have proper profiles: reachable, with handles, experience and a CV. Decision: a separate `professionals` table (not a business with a person's name) with a static profession list in `src/content/professions.ts` (36 professions in 11 groups, each with its licence body: PMDC, PEC, PCATP, Bar Council, ICAP, PNC). A profile carries headline, bio, city and area, workplace and service mode, phone, WhatsApp, optional public email, website, eight social handles, languages, skills, services with fees, experience, education, certifications, registration number, availability, a rate, a photo and a PDF CV (public or private). URLs: `/professionals` hub, `/professionals/[profession]`, `/professionals/[profession]/[city]` (noindex under three profiles), `/p/[slug]`. Profiles are created by the account that owns them (two per account), reviewed by an editor before going live, indexed in search as `professional` with a `person` intent that outranks businesses for "doctor in lahore" type queries, and rendered as Person JSON-LD with credentials and sameAs handles. Verified is a paid annual plan (Rs 4,900, `professional_plan` orders) or an editor's manual mark after checking the register and an identity document; the badge drops if name or profession changes on a free profile. Enquiries go to a per-profile lead table and the owner dashboard. Consequences: migration 0007 (new enum values on `search_entity_type` and `order_kind`, `orders.professional_id`); CV uploads add PDF support to storage; reviews for professionals reuse nothing yet and are a later addition.

## ADR-27 · Community: member posts with approval, comments and likes everywhere, moderation
**Accepted · 2026-09-15**
Context: The founder wants members to post jobs, listings, auctions and anything else (approved by an editor), with comments, replies and likes on posts and on our own content, member profiles, and a way to moderate. Decision (`src/db/schema/community.ts`, migration 0008): `member_profiles` (one per account: handle, display name, bio, photo, handles, verified and banned flags, counters, public page `/u/[handle]`), `posts` (five kinds with kind-specific JSON meta: job, listing, auction, question, discussion; statuses pending, published, rejected, hidden, closed; images; pinned; verified mark inherited from a verified member, business or professional), `bids` (auctions: minimum increment, end time, highest bid kept on the post), `comments` (one level of replies, on posts and articles; links from members hold for review, everything else goes up at once) and `reactions` (one like per user per target on posts, comments and articles). Posts by members wait for an editor; editors' own posts publish at once. Jobs close after 45 days, listings after 30, auctions at their end time (daily job). Anonymous readers never trigger a session request: forms and like state are gated on the sign-in hint cookie, which now carries the user id so "delete your own comment" needs no lookup, and one `/api/community/liked` call per page fills like state for signed-in readers. Search indexes published posts as `post` with a `market` intent for job and for-sale queries; job posts render JobPosting JSON-LD, listings and auctions Product, the rest DiscussionForumPosting. Moderation lives at `/admin/community` (approve, reject with a note, hide, pin, verify; comments; reports on posts, comments and members; verify or ban members, which hides their live content). Consequences: reports gained post, comment, professional and member targets; article pages gained a like and a comments section; rate limits per user on posts, comments and bids.

## ADR-28 · Shared inbox in admin on Resend receiving
**Accepted · 2026-09-15**
Context: The domain's MX points at Resend and receiving is verified, so mail to any @searchable.pk address lands there; the founder wants those mailboxes readable and answerable from admin rather than in a separate client. Decision (`src/lib/inbox.ts`, migration 0011): each received message is mirrored into `inbox_messages` keyed by Resend's id (sender, recipients, subject, snippet, plain text capped at 20k characters, attachment metadata, message-id) so admin has read and status state and a copy that outlives the provider's retention; HTML bodies and attachments are never stored and are fetched live when opened (HTML in a fully sandboxed iframe, attachments via a short-lived signed URL behind an editor-only redirect route). Two ways in: the signed `email.received` webhook at `/api/webhooks/resend` (Svix-style HMAC checked by hand, no dependency) and `syncInbox()` in the five-minute job runner as the safety net (one list call plus one detail call per unseen message, capped at 25). Messages are grouped by mailbox (the local part of our address) with new, replied, archived filters and a search. A reply goes out from the same mailbox through `sendEmail()` with In-Reply-To and References so it threads at the other end, quotes the original, counts against the transactional email budget, and marks the message replied. Consequences: `sendEmail()` accepts `from`, `replyTo` and `headers`; the Resend key must be full access for the receiving endpoints; `RESEND_WEBHOOK_SECRET` is optional (without it the endpoint refuses everything and the sync does the work); the contact form's `messages` queue stays separate because it is not email.

## ADR-29 · Admin API for automation, reusing the dashboard's actions
**Accepted · 2026-09-15**
Context: The founder wants a scheduled Claude task to run the site day to day: publish and update stories six times a day, record data, moderate queues, answer mail, assemble the newsletter. That needs a machine interface with exactly the safeguards a human editor gets. Decision: `Authorization: Bearer $ADMIN_API_KEY` is honoured inside `getSessionUser()` (constant-time compare, key at least 32 characters, never a browser session) and resolves to the first admin account, so every existing server action and `requireRole()` guard works unchanged from a route handler; the API is therefore a thin layer of JSON routes under `/api/admin/*` (`src/lib/admin-api.ts` wraps auth, JSON parsing, Zod errors and an `admin_api` analytics event per write) that speak in slugs (categories, cities, series, entities from `GET /reference`) and call `saveArticle`, `commitImport`, the moderation actions, `addDataPoint`, `runIngestion`, the inbox and newsletter libraries. Articles can take a photo as an Openverse query (credit recorded) or an openly licensed URL (`src/lib/media-import.ts`); nothing else. `GET /context` gives the automation the state it needs to avoid repeating itself. The playbook and the exact prompt for the scheduled task live in `docs/DAILY-TASK.md`. Consequences: no second auth system, no duplicated validation; the key is as powerful as the admin login and is rotated by changing one env var; every route is documented in `docs/ADMIN-API.md`.

## ADR-30 · The lens monogram
**Accepted · 2026-09-15**
Context: The founder asked for a better minimalist logo and favicon and chose, from seven vector directions, the lens monogram with a lowercase sans wordmark; the favicon is the black mark on a transparent ground, no tile. Decision: `src/components/brand.tsx` draws the mark inline (a ring, a square-cut handle, and the Newsreader 600 "S" as an outline path so no font has to load) in `currentColor`, and `Wordmark` sets "searchable" in Inter semibold, tight, with ".pk" in grey. `src/app/icon.svg` is the favicon (white variant under `prefers-color-scheme: dark`), `apple-icon.png` and the PWA PNGs sit on white with padding, `public/brand/mark.svg` and `mark-white.svg` are for third parties. The OG image carries the same mark. Consequences: the serif masthead is retired from the header and footer; the newsletter and transactional email keep the text wordmark until an image is worth the weight.

## ADR-31 · One comparison engine
**Accepted · 2026-09-15**
Context: The compare pages were two hand-written tables that did not work on phones and gave no help choosing. Decision: `src/components/compare/comparator.tsx` is the single engine; a page supplies its items and a config (id, title, price, search text, specs with an optional numeric getter and a "best" direction, filter groups, sorts, calculator actions). The engine renders the filter bar with a search box and result count, cards by default and a table on wide screens, a sticky tray of the picks with a jump to the side-by-side, and the side-by-side with the lowest price and the best value in each row marked. Filters, sort, search and picks live in the URL so a comparison can be shared and lands in Suspense for static pages. Consequences: cars and inverters are configs of about fifty lines each; a new comparison (credit cards, ACs) is a data file plus a config.

## ADR-32 · The charcoal and blue kit, tunable from admin
**Accepted · 2026-09-16**
Context: The founder chose a palette (charcoal 333333, slate 384650, teal 359EB4, blue 1A74A3, navy 00458E) to use across the site, asked for a proper brand kit, and wanted such settings in the admin rather than in code. Decision: five runtime tokens (`--brand-ink`, `--brand-slate`, `--brand-accent`, `--brand-link`, `--brand-primary`) are the only colours the design owns; `globals.css` mixes every other tone from them with `color-mix()` (the `brand-*` scale for links, eyebrows, buttons and tints; the `ink-*` scale for type and cool greys), so the utilities the code already used (`text-brand-700`, `bg-ink-900`, `--text`) change meaning without a rewrite. The root layout hoists the five values from the `site:settings` row into `<head>` after the stylesheet (`src/lib/site-settings.ts`), so a change in `/admin/settings` is live on the next page load with no deployment; the brand editor previews a candidate kit inside a `.brand-scope` that re-derives the scales locally. The same settings row carries the identity lines (tagline, contact, socials), the front-page rhythm (slides, seconds, ticker order) and homepage switches; the lead story, pins and breaking bar live in `front:page` and are set on `/admin/front-page` or through `POST /api/admin/front`. `/brand` publishes the kit (marks, palette, type, rules) and reads the same row. Primary buttons are navy, links and eyebrows blue, the carousel timer and live dot teal, type charcoal; the monochrome rules on radius, gradients and glass stand (ADR-15). Consequences: no theme files, no build for a colour change; `color-mix()` needs a 2023 browser (everything the site targets); the site name and fonts stay in code on purpose.

## ADR-33 · One grotesk, a square lens, five sections
**Accepted · 2026-09-17**
Context: The founder found the site old fashioned (serif headlines, a lens-and-serif-S monogram) and the top navigation too complicated (eight sections plus a date and tagline line). Decision: Geist is the only typeface (400 and 500 for text, 600 with -0.022em tracking for headlines; the `font-display` utility carries weight and tracking, `font-serif` is gone); the mark is a square lens (heavy square ring in the brand navy, square-cut handle in the teal accent, `src/components/brand.tsx`, also the favicon, PWA icons and social cards); the header is one 60 px row with five sections (News, Guides, Tools, Directory, Data), search, Newsletter and the account link. The directory panel holds businesses, professionals, cities and community; the data panel holds the daily pages, prices, currency, rates and comparisons. Consequences: no serif files ship; the OG renderer reads Geist TTFs from `src/app/og`; ADR-30's monogram is retired.

## ADR-34 · Daily pages: weather, prayer times, Islamic date
**Accepted · 2026-09-17**
Context: Semrush (pk) shows the largest recurring searches in Pakistan are daily utilities ("weather today" 2.2M a month, "islamic date today" 1M, "sunset today" 823k, "gold rate today" 550k, "petrol price today" 450k, "today psl match" 368k), not headlines. Decision: `/weather/[city]` (MET Norway Locationforecast, CC BY 4.0, cached 30 minutes per city, no key), `/prayer-times/[city]` (computed in `src/lib/today`, Karachi convention 18°/18°, Hanafi Asr, Shafi shown), `/islamic-date` (Umm al-Qura via Intl plus a Ruet-e-Hilal offset in the `today:hijri` settings row, set through `POST /api/admin/today`), and a `/today` hub; all ISR at 30 minutes, cities are the locations rows with coordinates. Consequences: no new dependency; the scheduled task keeps the Hijri offset right after each sighting announcement; MET Norway is credited on every weather page.

## ADR-35 · Living price lists
**Accepted · 2026-09-17**
Context: Semrush (pk) shows "<model> price in pakistan" is the largest cluster of low-competition searches in the country (dozens of phone models at 40k to 200k searches a month, KD 12 to 29; Honda 125 and CD 70 at 110k and 90k), held today by thin pages. Decision: one engine for mobiles, bikes and cars: items in settings rows `prices:<category>` (zod in `src/lib/prices-shared.ts`), maintained by the scheduled task through `POST /api/admin/prices` from the makers' official Pakistan lists, with server-kept price history; pages `/prices`, `/prices/[category]`, `/prices/[category]/[slug]` (brand or model, resolved at request time), Product JSON-LD, PTA tax computed for phones from the FBR slabs and the live dollar rate. Consequences: the task fills 20 to 40 models a run; categories stay noindex until ten models; no new dependency.

## ADR-36 · Compete where we can win
**Accepted · 2026-09-17**
Context: The founder wants the most readers, but a young site cannot outrank Dawn, Geo or ESPNcricinfo on the day's headline. Decision: the task checks Semrush KD and the SERP before writing a hot story (KD over 55 and national outlets on top means our angle or skip), and spends most of every run on gaps (KD under 35, real volume, weak answers), which is what the backlog holds. Consequences: fewer me-too stories, more pages that rank; the backlog is seeded from Semrush with real numbers only.

## ADR-37 · Content Signals and markdown negotiation
**Accepted · 2026-09-17**
Context: Cloudflare's AI diagnostics listed two missing quick wins. Decision: robots.txt is a route (`src/app/robots.txt/route.ts`) so it can carry `Content-Signal: search=yes, ai-input=yes, ai-train=yes` for every agent; `src/proxy.ts` rewrites requests that prefer `text/markdown` on article, guide, tool and data pages to their `/api/md` rendition with `Vary: Accept`. Consequences: agents get markdown at the canonical URL; readers see no change; the proxy runs only on those four path patterns.

## ADR-38 · Agent surface: OpenAPI, API catalog, MCP, skill, WebMCP
**Accepted · 2026-09-17**
Context: Being cited by assistants is the site's primary goal; Cloudflare's agent-readiness scan listed the discovery documents agents look for. Decision: one read-only, key-free surface in `src/lib/agent-api.ts` (search, data series, calculator schema and run, page markdown) exposed three ways: REST (`GET/POST /api/tools/{slug}`, existing data, search and md routes, described by `/openapi.json` and catalogued at `/.well-known/api-catalog`, with `Link` headers on the home page); a minimal MCP server at `/mcp` (Streamable HTTP, JSON-RPC, tools only, no dependency) with its card at `/.well-known/mcp/server-card.json`; and a SKILL.md published through `/.well-known/agent-skills/index.json`. A tiny inline script registers the same three actions with WebMCP in browsers that have `navigator.modelContext`. Not adopted, on purpose: OAuth/OIDC discovery, protected-resource metadata and auth.md (nothing here needs an agent to authenticate), A2A card and DNS-AID (no agent endpoint to advertise), Web Bot Auth (the site sends no bot traffic), and every commerce protocol (ACP, AP2, MPP, UCP, x402: nothing is sold to agents). Consequences: `/mcp` and `/api/tools/*` share the search rate limit; every answer carries the canonical url to cite.

## ADR-39 · Audit hardening: escape, sign, header, and keep the rate limiter in memory
**Accepted · 2026-09-17**
Context: A full audit two days after go-live found one critical path (any signed-in member could get script into an editor's browser through post bodies and profile bios rendered by the editor-grade Markdown renderer, and through owner-editable taglines in search snippets), several public RPCs (helpers exported from `"use server"` files), sequential invoice URLs that showed payer details to anyone, no security headers, and every tool page shipping all 31 calculators to the browser. Decision: (1) two renderers in `src/lib/markdown.ts`: `renderMarkdown` for editor content keeps raw HTML, `renderUserMarkdown` for member content shows it as text; both refuse every href scheme except http(s), mailto and tel and escape attributes, so no sanitiser dependency is added. Search escapes text before `ts_headline`. A member's edit to a live post returns it to the queue. (2) Only actions are exported from `"use server"` files; `ensureMemberProfile`, `canEditBusiness`, `canEditProfessional` and `publishDueArticles` moved to plain modules. (3) Invoice links carry an HMAC of the invoice number (`orderPath()`); the page and the payment-reference action accept that key, the buying account or an admin, and answer 404 otherwise. (4) `next.config.ts` sets nosniff, Referrer-Policy, Permissions-Policy and a CSP of `frame-ancestors 'none'; object-src 'none'; base-uri 'self'`, relaxed to `frame-ancestors *` on calculator pages for `?embed=1`; a script CSP is not workable with AdSense and is not attempted. (5) Cron routes compare the secret in constant time and are closed in production without one; the seed refuses its default admin password off local PGlite. (6) The tool runner loads one calculator with a dynamic import (`src/tools/load.ts`) and paints the server-computed result first. Not adopted: a shared-store rate limiter (Upstash) needs a dependency and an account; the in-memory limiter plus the five-attempt claim-code counter is enough until abuse is seen, and this entry is where that decision gets revisited. The Vercel region stays `hnd1`, next to the Supabase project. Consequences: member HTML never reaches the DOM; invoices in old emails (sent before this date) need the new link, which admin can copy from `/admin/orders`; every test in `tests/unit.test.ts` that pins these behaviours must stay green.

## ADR-40 · Admin API keys made in admin, not in the environment
**Accepted · 2026-09-19**
Context: One `ADMIN_API_KEY` in the host's environment served the scheduled editorial task. The founder wants to hand external tools (other automations, scripts, integrations) the full admin API without a login or an MCP, and to cut any one of them off without touching the others. Decision: an `api_keys` table (name, shown prefix, SHA-256 of the key, role `editor` or `admin`, maker, last use, `revoked_at`) and an admin-only page `/admin/api-keys` that makes a key (`spk_` plus 64 hex characters from Web Crypto, shown once) and revokes it. `getSessionUser()` honours a bearer that matches the environment key (acts as the first admin) or the hash of an unrevoked row (acts as the maker with the key's role, and only while the maker still holds at least that role: a demoted or deleted admin takes their keys down with them); `last_used_at` is written at most every five minutes. The admin API returns 503 only when neither kind of key exists. Not adopted: scopes per route (a role is enough until a tool needs less than the desk), key expiry (revocation is one click), and making keys through the API itself (a leaked key must not be able to mint more). Consequences: the key check costs one indexed lookup per request; the environment key stays as a bootstrap and for the migration's contract suite; Web Crypto was chosen so the module runs unchanged on Workers.

## ADR-41 · Cloudflare Workers through vinext, OpenNext as the fallback
**Accepted · 2026-09-19** (migration Phase 1; docs/INVENTORY.md)
Context: The site moves from Vercel to Cloudflare Workers to run at near-zero cost with everything in one account. Cloudflare's own guidance recommends vinext, a Vite plugin that reimplements the Next.js API surface, with the OpenNext adapter as the other supported path. Decision: vinext. `npx vinext check` reported 91 percent compatibility with one issue (`"type": "module"`, added) and the first full build passed with every route classified; the built Worker boots under `wrangler dev` and serves route handlers, static routes and the social card. `next dev` and `next build` keep working unchanged, so Vercel stays the production host until Phase 9. Configuration: `vite.config.ts` (page cache through the Workers Cache, no KV data cache because the free plan allows 1,000 KV writes a day), `wrangler.jsonc` (`nodejs_compat`, static assets binding). Runtime-specific code lives in exactly one module pair, `src/lib/platform.ts` (Node) and `src/lib/platform.workerd.ts` (Workers), swapped by a `pre` Vite plugin because vinext resolves `@/` paths before `resolve.alias`; the same plugin stubs the local database engine out of the Workers bundle. Not adopted: rewriting `node:crypto` callers to Web Crypto (the compatibility layer provides them), the Rate Limiting binding (only 10 or 60 second windows; ours run to a day). Fallback: if vinext fails a later phase, OpenNext's custom worker pattern takes the same `platform.workerd.ts`. Consequences: two build pipelines until cutover; `.next/types/validator.ts` is excluded from the type check because vinext rewrites the generated route types; the 10 ms CPU limit of the Workers free plan is measured on a deployed staging Worker before Phase 2 starts.

## ADR-42 · Image scaling in WebAssembly: photon plus libwebp
**Accepted · 2026-09-19**
Context: Uploads and imported photos were normalised and given 480 and 960 px renditions by sharp, a native binary that cannot run on Workers. Decision: `@cf-wasm/photon` (Rust image library compiled to WebAssembly) decodes and resizes; `@jsquash/webp` (libwebp compiled to WebAssembly) encodes, because photon's own WebP encoder is lossless and produced files six times larger. Measured on a 1800 px photo: the 960 and 480 px renditions came out at 41 KB and 13 KB against sharp's 38 KB and 13 KB, so URLs, widths and page weight are unchanged. Both modules are handed in by `src/lib/platform.ts` (compiled from disk on Node, imported as WebAssembly modules on Workers, where compiling from bytes is refused). The R2 write goes through the `MEDIA` bucket binding on Workers and keeps the hand-signed S3 PUT elsewhere. Not adopted: EXIF auto-orientation (sharp did it; photon has no EXIF reader; revisit if sideways uploads appear), Cloudflare Images (paid). Consequences: decode plus resize costs 200 to 500 ms of CPU per image, which the Workers free plan (10 ms per request) cannot run; image work therefore needs the Workers Paid plan (USD 5 a month, 30 seconds of CPU) or must leave the request path.

## ADR-43 · Images are resized by whoever has the bytes, never by a CPU-capped server
**Accepted · 2026-09-19** (supersedes the request-path half of ADR-42)
Context: The founder chose the Workers free plan (USD 0, 10 ms of CPU per request) over Workers Paid. Decoding and resizing one photo costs 200 to 500 ms even in WebAssembly, and rendering a social card about the same, so neither can run inside a request there. Decision: the server never resizes on a runtime that may not spend the CPU (`heavyComputeAllowed` in `src/lib/platform.ts`, false on Workers). (1) Browser uploads: `upload-client.ts` decodes once with `createImageBitmap`, draws the master and the 480 and 960 px renditions through a canvas and encodes WebP at the same qualities the server used (82 and 78), then sends all three; a browser without a WebP encoder falls back to the raw upload, which only a Node server accepts. (2) The automation posts the same three files to `POST /api/admin/media` as multipart; the JSON `{ query }` and `{ url }` imports answer 400 with instructions where resizing is not allowed, and `backfillArticlePhotos` does nothing there. (3) The server validates what it receives by reading WebP headers (`src/lib/webp.ts`, no decoding): container, master size against the variant, each rendition exactly the expected width. (4) Social cards: articles already use their photo; every other page shares one static card (`public/og-card.png`, rendered once from the Node route) where cards cannot be rendered, and `/og` serves that file so old links keep working. Not adopted: Cloudflare Images (paid), a third-party resizing proxy (a dependency on the request path of every image), keeping rendition creation in a Workflow (same 10 ms limit on the free plan). Consequences: browser-encoded files are a few percent larger than libwebp's; animated GIFs upload raw and are refused on Workers; photo-less hub and tool pages lose the headline on their social card on Workers; the WebAssembly pipeline of ADR-42 stays for Node (Vercel until cutover, scripts, the raw-upload fallback). Addendum, same day: the founder moved the account to Workers Paid, so `HEAVY_COMPUTE=1` in `wrangler.jsonc` turns server-side resizing and card rendering back on there; the browser and multipart paths stay because they are cheaper everywhere.

## ADR-44 · Workers Paid, one global page cache, one database client per render
**Accepted · 2026-09-19**
Context: The first staging deploy on the Workers free plan measured 94 to 278 ms of CPU per uncached page render against a 10 ms allowance that Cloudflare enforces once a Worker exceeds it "consistently", and 100,000 requests a day as a hard stop about three times the site's traffic. The founder chose Workers Paid (USD 5 a month, 30 s of CPU, 10M requests, one bill for every site on the account). Decision: (1) vinext's page cache moves from Workers Cache, which is one copy per data centre, to the Response Store in self-contained mode: rendered pages in an R2 bucket with a SQLite Durable Object as the index, one copy worldwide, Workers Cache still in front at each location. The bucket carries an Asia-Pacific location hint; R2's default put the first one in North America and every cache hit fetched its body from there (about 600 ms). (2) On Hyperdrive a database client is created per render through React's `cache()`, never kept across requests: a socket reused from a finished request hangs the next one, and a client per query made a cold page open ten connections to Tokyo. Route handlers, which render outside React, get a client per call. (3) Bindings are read inside functions, never at module scope. (4) Staging shares the production database in Phase 1 and is fenced: `JOBS_DISABLED`, `EMAIL_PROVIDER=none`, `NOINDEX`. Measured after the change: warm hit 14 to 58 ms wall, store hit 170 to 720 ms, cold render about 3.3 s of which 250 ms is CPU and the rest is round trips to the database until Smart Placement moves the Worker next to it. Not adopted: the KV data cache (1,000 writes a day on free, and the Response Store covers data caching too), a separate cache Worker (a second deploy for no gain at one site). Consequences: the free-plan levers of ADR-43 remain as optimisations; docs/FREE-TIER.md is rewritten at cutover (Phase 9) for the new bill; the old cache bucket `searchable-response-store-cache-bodies` empties itself through a one-day lifecycle rule and is deleted afterwards.

## ADR-45 · Cloudflare D1 (SQLite) replaces Postgres; FTS5 replaces tsvector and pg_trgm
**Accepted · 2026-09-19** (supersedes ADR-2, ADR-3 and the search index half of ADR-5; migration Phase 2)
Context: With the runtime on Workers, keeping Supabase meant a database a continent away from the Worker and its own free-tier ceilings, while D1 is included in the Workers plan and sits in the same region as the Worker. The plan's Open Decision 1 had a default of D1 and a criterion for staying on Postgres (heavy use of Postgres-specific features); that criterion was met, and the founder chose D1 anyway for one bill and one place. Decision: the schema moves to `drizzle-orm/sqlite-core` with the translations in docs/schema-notes.md (enums as text with CHECK constraints keeping their `enumValues` API, JSON as text, timestamps as epoch milliseconds, booleans as 0/1). Full-text search is an FTS5 external-content table over search_documents with column weights that mirror the old A/B/C vector, a trigram FTS5 table plus a JavaScript similarity function in place of pg_trgm, and a query translator in place of websearch_to_tsquery; triggers keep both indexes in step. search_documents gained an integer `seq` key because FTS5 needs a stable rowid. Run reports moved out of one settings value into an automation_reports table (the value had outgrown D1's 100 KB statement limit). The client is one Drizzle instance per isolate over the `DB` binding; scripts that need the database call the admin API of a running instance; `scripts/export-for-d1.ts` copies Supabase into D1 as idempotent upserts (types from information_schema, self-referencing tables ordered parents first) and verifies counts and random rows. Migrations live in `migrations/` and are applied by wrangler; PGlite, postgres-js, Hyperdrive and the Vercel crons leave with the `main` branch at cutover. Development runs on `npm run dev` (vinext with a local D1) using `wrangler.dev.jsonc`, which omits the Response Store Durable Object the dev server cannot host. Measured on staging: cold renders 0.4 to 2 s (3 to 6 s on Hyperdrive), zero exceptions, every route serving. Not adopted: PlanetScale Postgres through Hyperdrive (a second vendor and bill), Turso/libsql (another vendor), keeping a Node SQLite driver for scripts (nothing in Node needs the database any more). Consequences: two branches until cutover (`main` for Vercel, `d1` for Workers) with hotfixes cherry-picked; search ranking differs from Postgres and is tuned by eye in Phase 6 (stop words like "how to" already dilute prefix matches); D1 enforces foreign keys, which the import order respects; the Supabase project is paused after cutover and deleted a week later.

## ADR-46 · A scheduler Worker with four Cron Triggers, a fuel-price Workflow, and no KV
**Accepted · 2026-09-19** (migration Phases 3 and 4)
Context: On Vercel the site had two daily crons plus an external five-minute pinger, and the OGRA fuel revision on the 1st and 16th was watched by hand in the editorial slots. Decision: a separate 100-line Worker, `workers/scheduler`, holds the schedule and nothing else. Its Cron Triggers call the site's existing `/api/cron/publish` (every five minutes), `/api/cron/ingest?only=<market series>` (hourly), and `/api/cron/ingest` (daily, with the tools mirror and pruning) through a service binding with the shared `CRON_SECRET`; on the 1st and 16th at 15:00 and 18:00 PKT it starts `FuelPriceWorkflow`, one instance per day, whose first step ingests petrol and diesel and fails while nothing has changed so the platform retries five times, ten minutes apart doubling, until the notice lands; upserts on (series, date) and dated draft slugs make re-runs harmless. The site records every ingest in `ingest_runs` and exposes `GET /api/admin/ingest/status` (last run, last success, last value, error, hours stale per series) so a slot can check freshness before writing. The collector fetches only the sources a run asks for (it hit CoinGecko's rate limit when every source was read for two series). Not adopted: KV write-through for the data hub and KV for the press feeds (D1 sits next to the Worker and the Response Store caches fetches globally, so the brief's KV layer would add writes for no latency gain), a Workflow for the reindex (an admin job is enough at 650 documents), press or sitemap warming crons. Consequences: cron-job.org and `vercel.json` retire at cutover; the scheduler is deployed separately (`npm run deploy:scheduler`, `deploy:scheduler:production`) and needs the same `CRON_SECRET` as the site; a manual `POST /run/<cron>` on the scheduler fires any trigger for checks; migrations that Drizzle cannot express are reserved with `drizzle-kit generate --custom` so numbering stays aligned.

## ADR-47 · One policy matrix (`@jet/authz`), an authz CI guard, and Turnstile on the public forms
**Accepted · 2026-09-19** (migration Phase 5)
Context: who may do what lived in three places: `hasRole()` with a rank table in `src/lib/auth.ts`, `canEditBusiness`/`canEditProfessional`/`canViewOrder` with their own rules, and ad hoc checks in route handlers; nothing failed the build when a new action forgot to authorise. Sign-up and the seven public forms (contact, business and professional enquiries, pitch, newsletter, add-business, login) stood behind rate limits only. Decision: `packages/authz` (npm workspace `@jet/authz`, TypeScript source, no build step) holds `MATRIX` (resource by operation, each cell the least actor allowed plus an optional `own` condition), `can(actor, op, resource, {own})` and `atLeast(actor, role)`; `matrix.test.ts` writes out every cell including the denials. `src/lib/auth.ts` keeps its names: `hasRole` is `atLeast`, and `allowed(user, op, resource, ctx)` is `can` for a session; the ownership helpers call it. `scripts/check-authz.ts` runs in CI and fails when a `"use server"` file or an `src/app/api/**/route.ts` neither imports `@/lib/auth` (or a documented equivalent: `withAdminApi`, `cronAuthorized`, `verifyResendWebhook`) nor appears in its public list. Cloudflare Turnstile (managed mode, one widget per environment) guards the public forms: `<Turnstile/>` inside the form, the token passed to the action as `turnstile` and checked by `verifyTurnstile()` against siteverify; better-auth's `captcha` plugin checks the `x-captcha-response` header on sign-up and sign-in. The site key is a runtime var (`TURNSTILE_SITE_KEY`, written into a meta tag by the root layout) rather than a `NEXT_PUBLIC_` value, so one build serves staging and production; without `TURNSTILE_SECRET` the check is skipped, so local development and tests need no keys. better-auth `trustedOrigins` lists the workers.dev host and localhost, because the Worker answers on more than its `baseURL`. Not adopted: an `automation` actor (a key acts as the user who made it, ADR-40), the brief's `approvedClaim` condition (`own` is established by the caller, which already resolves claims), Turnstile on signed-in forms (posts, comments, reviews, claims keep rate limits). Consequences: a new resource or operation is a change to `MATRIX` and its test before any handler; forms that add a Turnstile widget must sit the widget inside the `<form>`; `auth-client` no longer sets a `baseURL` (a build-time value pointed staging at localhost).

## ADR-48 · Errors as fingerprints in D1 with one email each, product events in Analytics Engine
**Accepted · 2026-09-19** (migration Phase 7)
Context: the site logged every uncaught error as an `analytics_events` row and showed a grouped list on `/admin/system`; nobody was told when something new broke, and the brief asked for an error tracker with fingerprints and a first-occurrence alert, plus product analytics on Cloudflare. Decision: `packages/errors` (`@jet/errors`) is the shared, pure part: `describe(err)` picks name, message and the first application frame, `normaliseFrame` strips line and column numbers and hosts, `normaliseRoute` collapses ids, and `fingerprint()` is the first 16 hex characters of sha256(route | name | frame), so a redeploy or a different id does not mint a new row. `src/lib/errors.ts` upserts `error_fingerprints` (count, first and last seen, latest message and sample) and, when the returned count is 1, sends one email to `EDITORIAL_EMAIL` through the existing budgeted mailer. Sources: `onRequestError` in `src/instrumentation.ts` (server) and `POST /api/client-errors` from `src/app/error.tsx` (browser, rate limited). No digests, no Tail Worker: the hook already sees every server error and a Tail Worker would add a deploy for the same rows. Product events go to Workers Analytics Engine through `track()` (`src/lib/track.ts`, a no-op without the binding): searches with intent and result count, calculator runs by slug, business page views (a client beacon, because pages are served from the edge cache; it also bumps `view_count`, which nothing incremented before), newsletter sign-ups, community posts. `/admin/metrics` reads the SQL API with `CF_ANALYTICS_TOKEN`, cached ten minutes, and shows `ingest_runs` freshness beside it. Page views stay with Clarity and Cloudflare Web Analytics (automatic injection on the proxied zone, a dashboard toggle). `/admin/system` reads the database size from D1's `size_after` meta; `pragma_page_count()` is not allowed on D1. Consequences: `analytics_events` no longer holds errors and the track route no longer accepts an `error` event; fingerprints unseen for 90 days are pruned with the other old rows; `/admin/metrics` says "not configured" until the API token exists; every new event is a `TrackEvent` member and a column on the metrics page.

## ADR-49 · `@jet/email`: a provider seam over Resend's REST API, bounces close subscriptions
**Accepted · 2026-09-19** (migration Phase 8)
Context: `src/lib/email.ts` mixed the sending budget with three inline transports (the Resend SDK, an `.eml` outbox written with `node:fs`, a console drop), and a bounce or complaint at Resend changed nothing in the newsletter table. Decision: `packages/email` (`@jet/email`) holds the provider interface (`{ name, send(message) }`) with `resendProvider(apiKey, fetch)` (one POST to `api.resend.com/emails`, tested with an injected fetch) and `consoleProvider(label)`. `sendEmail()` keeps its signature, budget and kinds, and only picks a provider by `EMAIL_PROVIDER`. The `resend` SDK is removed (the inbox already read received mail over REST). The webhook handles `email.bounced` and `email.complained` by setting the recipient's `newsletter_subscribers.status` to `bounced`; the issue sender already mails `active` subscribers only. Staging has its own Resend webhook (created through the Resend connector) so signed events can be replayed against the staging Worker. Not adopted: Resend Audiences and Broadcasts for the newsletter (the local table has double opt-in, topics, frequency and a resumable sender that respects the shared 100-a-day allowance), a second provider today (Cloudflare Email Service would be one file here when wanted), the `.eml` outbox (Workers have no disk; the console line carries the same envelope). Consequences: any new transport is a provider in the package; a bounced address is never mailed again until the subscriber re-confirms; `docs/FREE-TIER.md` keeps the Resend allowances.

## ADR-50 · Cloudflare Images does the resizing on Workers
**Accepted · 2026-09-19** (cutover day)
Context: the first editorial run after cutover could not import a single photo. Decoding a JPEG with photon in WebAssembly inside the Worker's 128 MB exhausted memory (error 1102): a 15 MB camera original from Commons by a wide margin, and even a 300 KB file once the isolate was warm with the server bundle, the Response Store and two WebAssembly codecs. Decision: when the `IMAGES` binding exists (`wrangler.jsonc`, `"images": { "binding": "IMAGES" }`), `storeImage()` streams the source to Cloudflare Images (`info` for the dimensions, `input().transform({ fit: "scale-down" }).output({ format: "image/webp" })`) for the master and the two renditions, and the Worker only stores bytes in R2; the photon path stays for Node (development) and as the fallback without the binding. Same files, same names, same media row. Included: 5,000 unique transformations a month, then USD 0.50 per 1,000; the site imports a few hundred photos a month. Not adopted: a separate image Worker (isolation for the same WebAssembly problem, one more deploy), shrinking sources before decoding (Commons only serves a fixed set of widths and the master wants 1800 px), moving all resizing to the automation (ADR-43's browser and multipart path still works and is still the route for hosts without the binding). Consequences: `HEAVY_COMPUTE` no longer gates uploads on Workers with the binding; `backfillRenditions` still decodes with photon and should move to the binding if it is ever run at scale on Workers.

## ADR-51 · News photos must describe the story, or there is no photo
**Accepted · 2026-09-19**
Context: the founder saw stories with loosely related photos (a shrine in Kohat on a story about the Kohat attack, a skyline for a court ruling). The import took the first usable result for a query that was often a topic rather than a subject, and the backfill fell back to a generic photo per category. Real news photography is never openly licensed, so the choice is between the right Commons or Openverse photo of the actual subject and no photo at all. Decision: `relevanceScore()` in `src/lib/open-images.ts` scores every candidate's own text (title, tags, description, categories) against the story's named entities (a full match counts most, a distinctive last word half), the query's specific words and the headline's, and pushes stock subjects (flags, banknotes, skylines, maps, logos) under the bar unless the query asked for them. Candidates are tried best first. For `kind: "news"` (`strict`), a candidate below `STRICT_MIN_SCORE` is not tried, the generic fallback query is skipped, and `findAndImport` returns null: the story publishes without a photo and the API response carries a note; cards and the article page already render without an image, and the social card does not depend on a photo. The backfill job applies the same rule to news. The task prompt now requires `entities` on every news story, most specific first, and tells the task to bring the subject's own image (handout, official page) by URL when the search comes back empty, never a vaguer query. Guides and explainers keep the lenient path (a scene for the topic is fine). Consequences: fewer news stories carry a photo, and the ones that do show their subject; `tests/photo-relevance.test.ts` holds the first live day's cases; the bar (2.0) and the stock list are the two knobs.

## ADR-52 · One branded shell for every email
**Accepted · 2026-09-19**
Context: twenty transactional templates and the newsletter each carried their own chrome, most of it from before the brand kit (Georgia serif, a green ".pk", no mark), and the founder asked for professional, well-branded mail. Decision: `src/lib/email-layout.ts` renders one shell (square-lens mark from `/icon-192.png`, "searchable.pk" wordmark with the navy ".pk", a charcoal rule, the content with inline styles, an optional navy button, a footer with the address and the right links), on the site's tokens (ADR-32) with a system sans stack in place of Geist, tables and inline styles only. `sendEmail()` wraps any `html` that is not already a full document, so every existing template is branded without a call-site change; templates may pass `preheader`, `kicker` and `cta`. The newsletter uses the same shell with a Daily or Weekly kicker, the date line, and manage, unsubscribe and web links in the footer. Not adopted: React Email or MJML (a dependency and a build step for one layout), per-template art, dark-mode variants (mail clients handle the light card acceptably). Consequences: a new email is content plus a subject; the shell owns identity; `claims.ts` `layout()` now returns only a heading and the body.

## ADR-53 · Motion: two curves, short, and off under reduced motion
**Accepted · 2026-09-19**
Context: the site had no motion beyond the carousel timer; the mobile menu appeared, the hero flipped, panels popped. The founder wanted a sleek, minimal, premium feel without gradients, glass or anything that slides across the screen (ADR-15). Decision: two curves site-wide, defined once in `globals.css`: a 160 ms ease for state (colour, border, opacity, a 1.5 percent press on buttons) and a soft settle (`cubic-bezier(0.22, 1, 0.36, 1)`) for entrances: pages fade up 6 px over 360 ms, panels (mega menu, search suggestions, new live-feed rows, `details`) fade down 6 px over 220 ms, the mobile sheet fades and settles over 260 ms and is always mounted so it can, stacked layers in the hero carousel cross-fade over 480 ms (`layer-fade` utilities) instead of flipping visibility, and photos inside links drift 2.5 percent over 700 ms with the Img box clipping them. Everything collapses to near zero under `prefers-reduced-motion`. The router kept scroll position across navigations (a Vite runtime gap), so `ScrollToTop` in the root layout resets to the top on a pathname change except after back or forward or with a hash. Not adopted: a motion library, shared-element transitions, parallax, skeleton shimmer. Consequences: new interactive surfaces use `panel-enter` or `layer-fade`, never their own timing; durations live in the stylesheet only.

